"use strict";
let userName = ['admin', 'shoaib', 'abid', 'sajid', 'sabir'];
if (userName.length === 0) {
    console.log(' We need to find some users!');
}
else {
    userName = [];
    console.log('Remove all of the usernames from your array, ' + userName.length);
}
